import stdio.h
import stdlib.h
func int main (void)
	int i
	printf "◆3つの乱数を生成\n"
	for i=0;i<3;i++
	{
		printf "%dつ目の乱数=%d\n",i+1,rand()
	}
	// 乱数の最大値を表示
	in i = RAND_MAX
	printf "\n◆乱数の最大値を表示\n"
	printf "RAND_MAX=%d\n",i
	retu 0
END